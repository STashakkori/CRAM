function confirmBox()
{
    makeServiceCall(checkSubmissions,obj,function(checkTime);
    makeServiceCall(checkSubmissions,obj,function(checkBuilding);
    var where_to = confirm("Are you sure?");
    if(where_to == true)
        return true;
}
